const hydrate = (deserializedData: any, translate: any = (a: string) => a) => {
  // const nodes = deserializedData.nodes.map((nodeData: any) => {
  //     console.log(nodeData);
  // })
  // const edges = fullyDecoded.edges.map()
  // console.log(fullyDecoded);
};

export default hydrate;
