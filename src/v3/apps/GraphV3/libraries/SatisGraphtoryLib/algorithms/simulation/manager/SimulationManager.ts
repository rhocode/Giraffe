export default class SimulationManager {
  private ticker = new PIXI.Ticker();

  // T he tickspeed is set based on 60/ MAX_BELT_SPEED
  private readonly tickSpeed = 50;

  createSimulatedObject(slug: string) {

  }
}